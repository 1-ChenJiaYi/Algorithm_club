const rl = require('readline').createInterface({ input: process.stdin });

const lines = [];
rl.on('line', line => {
    lines.push(line);
});
const main = () => {
    // 书写输入的数据逻辑，将输入的数据逻辑传入解决函数     
    
    solve() 
}

const solve = () => {
    
}

rl.on('close', () => {    
    main()     
}); 
